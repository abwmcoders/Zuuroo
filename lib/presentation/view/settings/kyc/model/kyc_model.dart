class KycModel {
  final String first, last, middle, dd, mm, yyyy, bvn;

  KycModel({
    required this.first,
    required this.last,
    required this.middle,
    required this.dd,
    required this.mm,
    required this.yyyy,
    required this.bvn,
  });
}
